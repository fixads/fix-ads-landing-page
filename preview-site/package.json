{
  "name": "fixads-preview",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-sites.mjs",
    "dev": "npx netlify-cli@latest dev",
    "serve": "npx serve -s . -l 4173",
    "build:sites": "node scripts/build-sites.mjs",
    "build:netlify": "node scripts/build-netlify.mjs"
  },
  "devDependencies": {
    "@netlify/edge-functions": "latest",
    "typescript": "latest"
  }
}
